# docs

- **[`deployment.md`](./deployment.md)** — how to run the backend,
  frontend, and desktop shell on Linux, Windows, and macOS (Docker
  Compose, published GHCR images, and native/no-Docker instructions for
  each OS).

Also planned, not yet populated:

- `api/` — OpenAPI/API reference exports (populated once the API surface
  grows beyond `/health`, `/ws/chat`, `/ws/voice`, see `ROADMAP.md`).
- `diagrams/` — architecture and sequence diagrams referenced from
  `ARCHITECTURE.md`.

See the root [`ARCHITECTURE.md`](../ARCHITECTURE.md) for the diagrams that
exist so far (inline Mermaid).
