# docs

Generated/maintained documentation that doesn't belong inline in code:

- `api/` — OpenAPI/API reference exports (populated once the API surface
  grows beyond `/health`, see `ROADMAP.md`).
- `diagrams/` — architecture and sequence diagrams referenced from
  `ARCHITECTURE.md`.

Currently empty — see the root [`ARCHITECTURE.md`](../ARCHITECTURE.md) for
the diagrams that exist so far (inline Mermaid).
